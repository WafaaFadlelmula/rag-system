# requirements-backend.txt — FastAPI server (Render / Railway / Fly.io)
# ─────────────────────────────────────────────────────────────────────────────
# Production API dependencies only.
# Does NOT include docling (PDF parsing — ingestion pipeline only)
# or streamlit (frontend only).
#
# Memory note: sentence-transformers (PyTorch) removed in favour of Cohere Rerank API.
# Total RSS at runtime is now ~120 MB — well within Render's free 512 MB tier.
# ─────────────────────────────────────────────────────────────────────────────

# API framework
fastapi==0.129.0
uvicorn==0.41.0

# OpenAI (LLM + embeddings)
openai==2.21.0
httpx==0.28.1

# Vector database
qdrant-client==1.16.2

# Retrieval
rank-bm25==0.2.2
cohere==5.15.0
numpy==2.2.6

# Data validation
pydantic==2.12.5
pydantic-settings==2.12.0

# Environment
python-dotenv==1.2.1
